module.exports = {
  // Formatting rules
  semi: true, // Use semicolons
  singleQuote: true, // Use single quotes instead of double
  trailingComma: 'all', // Add trailing commas wherever possible (ES5+)
  tabWidth: 2, // Number of spaces per indentation level
  printWidth: 100, // Line wrap at 100 characters
  bracketSpacing: true, // Print spaces between brackets in object literals
  arrowParens: 'always', // Always include parens for arrow functions
  endOfLine: 'lf', // Use line feed only (for cross-platform consistency)
  proseWrap: 'preserve', // Don't alter markdown line wrapping
  quoteProps: 'as-needed', // Only quote object keys when necessary
  jsxSingleQuote: false, // Use double quotes in JSX

  // Optional:
  plugins: [],
};
